

核心创新点的实现位置如下：

## 核心代码位置

### 1. **时间注意力权重计算**
位置：`MA_SNN/module/Attention.py`

```7:33:MA_SNN/module/Attention.py
class TimeAttention(nn.Module):
    def __init__(self, in_planes, ratio=16, fc=False):
        super(TimeAttention, self).__init__()
        if fc ==True:
            self.avg_pool = nn.AdaptiveAvgPool1d(1)
            self.max_pool = nn.AdaptiveMaxPool1d(1)

            self.sharedMLP = nn.Sequential(
                nn.Conv1d(in_planes, in_planes // ratio, 1, bias=False),
                nn.ReLU(),
                nn.Conv1d(in_planes // ratio, in_planes, 1, bias=False),
            )
        else:
            self.avg_pool = nn.AdaptiveAvgPool3d(1)
            self.max_pool = nn.AdaptiveMaxPool3d(1)

            self.sharedMLP = nn.Sequential(
                nn.Conv3d(in_planes, in_planes // ratio, 1, bias=False),
                nn.ReLU(),
                nn.Conv3d(in_planes // ratio, in_planes, 1, bias=False),
            )
        self.sigmoid = nn.Sigmoid()

    def forward(self, x):
        avgout = self.sharedMLP(self.avg_pool(x))
        maxout = self.sharedMLP(self.max_pool(x))
        return self.sigmoid(avgout + maxout)
```

### 2. **权重乘到输入数据（核心创新点）**
位置：`MA_SNN/module/Attention.py` 的 `TA` 类

```163:181:MA_SNN/module/Attention.py
class TA(nn.Module):
    def __init__(self, timeWindows, channels, stride=1, t_ratio=16, fc=False):
        super(TA, self).__init__()

        self.relu = nn.ReLU(inplace=True)

        # self.ca = ChannelAttention(channels)
        self.ta = TimeAttention(timeWindows, t_ratio, fc)
        # self.sa = SpatialAttention()

        self.stride = stride

    def forward(self, x):
        out = self.ta(x) * x
        # out = self.ca(x) * out  # 广播机制
        # out = self.sa(x) * out  # 广播机制

        out = self.relu(out)
        return out
```

关键行：第176行 `out = self.ta(x) * x`，将时间注意力权重与输入相乘，抑制某些时间步的输入。

### 3. **应用到LIF神经元**
位置：`MA_SNN/module/LIF_Module.py`

```86:91:MA_SNN/module/LIF_Module.py
        outputsum = output.reshape(b, t, -1)

        if self.attention_flag == "no":
            data = outputsum
        else:
            data = self.attention(outputsum)
```

被抑制后的数据传入 LIF 神经元：

```93:96:MA_SNN/module/LIF_Module.py
        for step in range(list(data.size())[1]):
            out = data[:, step, :]
            for layer in self.network:
                out = layer(out)
```

### 4. **LIF神经元的膜电位更新**
位置：`MA_SNN/module/LIF.py`

```87:93:MA_SNN/module/LIF.py
        # Step 1: accumulate and reset,spike used as forgetting gate
        u = self.h + input

        x_ = u - self.Vthres
        x = self.spikeActFun(x_)
        self.h = x * self.Vthres + (1 - x) * u
        self.h = self.h * self.alpha + self.beta
```

## 实现机制说明

虽然权重没有直接乘到膜电位，但通过以下方式间接抑制脉冲发放：
- 时间注意力权重在输入层面应用（`self.ta(x) * x`），抑制某些时间步的输入。
- 被抑制的输入进入 LIF 后，会降低膜电位累积速度。
- 结果等价于抑制膜电位增长，减少脉冲发放。

这是 Attention-SNN 的核心创新：通过可训练的时间步权重选择性地抑制输入，间接控制膜电位和脉冲发放。

他的时间注意力模块在`MA_SNN/module/Attention.py`中，实现如下：
```7:33:MA_SNN/module/Attention.py
class TimeAttention(nn.Module):
    def __init__(self, in_planes, ratio=16, fc=False):

是一个和新的输入有关的，这个和我想要的不太一样
我需要重新做数据切分然后只有一个T这样的参数
最后使用这个注意力模块，这样就可以得到一个T*C的注意力权重，然后就可以直接做硬门控

其实我想要的是一个动作按照时间点在整个动作中占得时间位置，然后得到一个T*C的注意力权重，然后就可以直接做硬门控
但是这里是一个和新的输入有关的，这个和我想要的不太一样
我将重新做数据切分然后只有一个T这样的参数
最后使用这个注意力模块，这样就可以得到一个T*C的注意力权重，然后就可以直接做硬门控
这里的dt我会根据T来重新计算

突然感觉也不能直接简单这样做除法，可能还是dt但是时间步采集要求均匀，这样可以保证每帧不会过强过弱，
但是问题是对于dvs数据来说，是不是同一个动作整体的产生的亮暗次数是相同的，
所以即使数据集时间不一样长，但是动作是一样的，这里的脉冲强度就应该相同的呢
原有的居然没有做归一化，那么不同的数据获取到的是不同的脉冲强度，这个需要重新考虑